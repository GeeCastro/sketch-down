version: '3.8'
services:
  app:
    build: .
    ports:
      - "8081:8080"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8080
      - DATA_DIR=/app/data
      - WEB_DIST=/app/web/dist
    restart: unless-stopped
